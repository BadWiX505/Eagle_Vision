// Bounding boxes are now streamed via WebSocket — see useDetectionStream.
// This stub is kept so existing import sites compile without changes.
export function useBBoxStream(_intervalMs?: number) {
  // no-op
}
